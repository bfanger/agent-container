volumes:
  pnpmstore:
services:
  dev:
    hostname: dev
    user: 1001:1001
    build:
      context: .
    volumes:
      - ./projects:/user/projects
      - pnpmstore:/user/.pnpm-store
    ports:
      - 5173:5173
      - 4096:4096
